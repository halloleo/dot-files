# some needed var constants
export WINHOME=`cygpath -p -w "${HOME}"` # HOME in non-cygwin style

# machine-depended env vars
export LC_ALL=C
export GIT_EDITOR=OpenInEmacsWaitCygwin.sh

# munch unix-style path lists into windows style
export PYTHONPATH=`cygpath -p -w "$PYTHONPATH"`

# alaises
alias python="python -i" #cheating here for tty issues in cygwin bash+emacs
alias mklink="cmd.exe /c mklink" 
alias k9="kill -9"
alias ssh2cbrfus07="plink -pw quad34 broleo@CBRFUS07"

. ~/.protected/bashrc_acc
