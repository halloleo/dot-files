[user]
	name = halloleo
	email = git@halloleo.dot.hailmail.dot.net
[core]
	excludesfile = ~/.gitignore_global
[github]
	user = halloleo
[credential]
	helper = store
[alias]
	cf = config
	cg = config --global
	st = status
        ls = ls-tree -r --name-only
[man "headman"]
	cmd = headman
